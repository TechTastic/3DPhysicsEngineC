#pragma once
/*#include "../../libs/mathc/mathc.h"

typedef struct PhysicsBody {
    mfloat_t moment_of_inertia_tensor[MAT3_SIZE];
    mfloat_t rotation[QUAT_SIZE];
    mfloat_t position[VEC3_SIZE];
    mfloat_t linear_momentum[VEC3_SIZE];
    mfloat_t angular_momentum[VEC3_SIZE];
    mfloat_t mass;
} physicsbody_t;

physicsbody_t create_new_body(mfloat_t position[VEC3_SIZE], mfloat_t rotation[QUAT_SIZE], mfloat_t mass);

mfloat_t get_transitional_velocity(physicsbody_t body);

mfloat_t get_angular_velocity(physicsbody_t body);*/